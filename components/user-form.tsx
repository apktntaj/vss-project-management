'use client'
import { useRouter } from 'next/navigation'
import { useState } from 'react'
export function UserForm(){const router=useRouter();const [error,setError]=useState('');const [saving,setSaving]=useState(false);async function submit(form:FormData){setSaving(true);const r=await fetch('/api/users',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(form))});setSaving(false);if(!r.ok){setError((await r.json()).error||'Tidak dapat membuat pengguna');return}router.push('/users');router.refresh()}return <form action={submit} className="card max-w-xl p-6"><label className="label block">Nama lengkap<input required name="name" className="input"/></label><label className="label mt-5 block">Email<input required name="email" type="email" className="input"/></label><label className="label mt-5 block">Password<input required name="password" minLength={8} type="password" className="input"/></label><label className="label mt-5 block">Peran<select name="role" className="input"><option value="STAFF">Staff</option><option value="SUPERVISOR">Supervisor</option></select></label>{error&&<p className="mt-4 text-sm text-rose-700">{error}</p>}<button disabled={saving} className="btn-primary mt-6">{saving?'Menyimpan…':'Buat pengguna'}</button></form>}
