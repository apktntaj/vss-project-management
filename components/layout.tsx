'use client'
import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { signOut, useSession } from 'next-auth/react'
import { FolderKanban, LayoutDashboard, Users, LogOut, Plus } from 'lucide-react'
const links = [{ href: '/', label: 'Dashboard', icon: LayoutDashboard }, { href: '/projects', label: 'Projects', icon: FolderKanban }]
export function AppShell({ children }: { children: React.ReactNode }) { const pathname = usePathname(); const { data } = useSession(); const menu = data?.user?.role === 'SUPERVISOR' ? [...links, { href: '/users', label: 'Pengguna', icon: Users }] : links; return <div className="min-h-screen md:flex"><aside className="flex w-full flex-col bg-slate-950 text-slate-300 md:fixed md:inset-y-0 md:z-20 md:w-64"><div className="px-6 py-6"><p className="text-lg font-bold text-white">VSS <span className="text-indigo-400">Projects</span></p><p className="mt-1 text-xs text-slate-500">Operational control center</p></div><nav className="flex gap-1 overflow-x-auto px-3 pb-3 md:block md:space-y-1">{menu.map(({ href, label, icon: Icon }) => { const active = href === '/' ? pathname === '/' : pathname.startsWith(href); return <Link key={href} href={href} className={`flex shrink-0 items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-medium ${active ? 'bg-indigo-600 text-white shadow-lg shadow-indigo-950/40' : 'hover:bg-slate-800 hover:text-white'}`}><Icon size={18}/>{label}</Link> })}</nav><div className="mt-auto hidden border-t border-slate-800 p-4 md:block"><p className="truncate text-sm font-medium text-white">{data?.user?.name}</p><p className="mt-0.5 text-xs text-slate-500">{data?.user?.role}</p><button onClick={() => signOut()} className="mt-3 flex items-center gap-2 text-xs hover:text-white"><LogOut size={14}/> Keluar</button></div></aside><main className="min-w-0 flex-1 md:ml-64"><header className="sticky top-0 z-10 flex items-center justify-between border-b bg-white/90 px-5 py-3.5 backdrop-blur md:px-8"><p className="text-sm font-medium text-slate-500">Project Management · Jakarta</p><Link href="/projects/new" className="btn-primary gap-2"><Plus size={16}/> Project baru</Link></header><div className="mx-auto max-w-[1440px] p-5 md:p-8">{children}</div></main></div> }
