'use client'
import Link from 'next/link'
import { useEffect, useState } from 'react'
import { getJob } from '@/lib/indexeddb'
import { StatusBadge } from '@/components/status-badge'
import { JobDetailActions } from '@/components/job-detail-actions'
import { Stages } from '@/components/stages'
import { formatDate } from '@/lib/utils'
export default function JobDetail({params}:{params:{id:string}}){return <JobDetailClient id={params.id}/>}

function JobDetailClient({id}:{id:string}){const [job,setJob]=useState<Awaited<ReturnType<typeof getJob>>>(null);useEffect(()=>{getJob(id).then(setJob)},[id]);if(!job)return <p className="card p-6 text-sm text-slate-500">Memuat job lokal...</p>;return <><div className="mb-7 flex items-start justify-between"><div><div className="flex gap-2"><h1 className="text-2xl font-bold">{job.jobNumber}</h1><StatusBadge status={job.status}/></div><p className="mt-1 text-sm text-slate-500">Diperbarui {formatDate(job.updatedAt)}</p></div><Link href={`/jobs/${job.id}/edit`} className="btn-secondary">Edit job</Link></div><div className="grid gap-6 lg:grid-cols-3"><div className="space-y-6 lg:col-span-2"><section className="card p-5"><h2 className="font-semibold">Informasi shipment</h2><dl className="mt-4 grid gap-4 text-sm sm:grid-cols-2"><div><dt className="text-slate-500">Klien</dt><dd className="mt-1 font-medium">{job.clientName}</dd></div><div><dt className="text-slate-500">Tipe</dt><dd className="mt-1 font-medium">{job.type}</dd></div><div><dt className="text-slate-500">Jenis muatan</dt><dd className="mt-1 font-medium">{job.shipmentMode || '—'}</dd></div><div><dt className="text-slate-500">Nomor B/L</dt><dd className="mt-1 font-medium">{job.blNumber || '—'}</dd></div><div><dt className="text-slate-500">Shipper / exhibitor</dt><dd className="mt-1 font-medium">{job.shipper || '—'}</dd></div><div><dt className="text-slate-500">Consignee</dt><dd className="mt-1 font-medium">{job.consignee || '—'}</dd></div><div><dt className="text-slate-500">Notify party</dt><dd className="mt-1 font-medium">{job.notifyParty || '—'}</dd></div><div><dt className="text-slate-500">Agent</dt><dd className="mt-1 font-medium">{job.agent || '—'}</dd></div><div><dt className="text-slate-500">Shipping line</dt><dd className="mt-1 font-medium">{job.shippingLine || '—'}</dd></div><div><dt className="text-slate-500">AWB</dt><dd className="mt-1 font-medium">{job.awbNumber || '—'}</dd></div><div><dt className="text-slate-500">PIC</dt><dd className="mt-1 font-medium">{job.assignedTo?.name || 'Belum ditugaskan'}</dd></div></dl>{job.cargoDescription && <div className="mt-5"><dt className="text-sm text-slate-500">Deskripsi barang</dt><dd className="mt-1 whitespace-pre-wrap text-sm">{job.cargoDescription}</dd></div>}{job.cargoDetails && <div className="mt-4"><dt className="text-sm text-slate-500">Detail barang</dt><dd className="mt-1 whitespace-pre-wrap text-sm">{job.cargoDetails}</dd></div>}{job.journeyDetails && <div className="mt-4"><dt className="text-sm text-slate-500">Detail perjalanan</dt><dd className="mt-1 whitespace-pre-wrap text-sm">{job.journeyDetails}</dd></div>}{job.notes && <p className="mt-5 rounded-lg bg-slate-50 p-3 text-sm text-slate-600">{job.notes}</p>}</section><Stages jobId={job.id}/><section className="card p-5"><h2 className="font-semibold">Dokumen</h2><p className="mt-3 text-sm text-slate-500">Dokumen akan ditambahkan pada iterasi MVP berikutnya.</p></section></div><aside><JobDetailActions id={job.id} token={job.trackingToken}/></aside></div></>}
